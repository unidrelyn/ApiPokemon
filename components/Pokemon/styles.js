import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        backgroundColor: '#000',
        height: '100%'
    },
    pokemonImage: {
        height: 250,
        width: '100%'
    },
    nameOfPokemon: {
        fontSize: 25,
        textAlign: 'center',
        color: '#fff'
    }
})

export default styles;
